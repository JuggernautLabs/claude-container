## Install Protocol

**Never run `./install.sh` without asking the user first.** The install overwrites the stable `claude-container` command. Always confirm before installing.

Similarly, never run `claude-container` or `claude-container-dev` commands that start/stop/modify containers or sessions without asking. Read-only commands (repos, session show, status, --dry-run) are fine.
